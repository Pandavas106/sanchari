// Common Components
export { default as Navbar } from './common/Navbar'
export { default as BottomNavbar } from './common/BottomNavbar'
export { default as LoadingSpinner } from './common/LoadingSpinner'
export { default as SearchModal } from './common/SearchModal'
export { default as NotificationCenter } from './common/NotificationCenter'
export { default as ProtectedRoute } from './common/ProtectedRoute'
export { default as SignOutModal } from './common/SignOutModal'
export { default as UserMenu } from './common/UserMenu'
export { default as SessionTimeout } from './common/SessionTimeout'
export { default as DataSeeder } from './common/DataSeeder'

// Explore Components
export { default as DestinationCard } from './explore/DestinationCard'
export { default as FilterModal } from './explore/FilterModal'
export { default as CategoryFilter } from './explore/CategoryFilter'
export { default as TrendingCard } from './explore/TrendingCard'
export { default as SortDropdown } from './explore/SortDropdown'
export { default as ViewToggle } from './explore/ViewToggle'

// Settings Components
export { 
  PersonalInfoModal, 
  PaymentMethodsModal, 
  PrivacySecurityModal, 
  LanguageCurrencyModal, 
  HelpSupportModal 
} from './settings'

// Dashboard Components
// (Will be added when dashboard-specific components are created)

// Cart Components
// (Will be added when cart-specific components are created)

// Profile Components
// (Will be added when profile-specific components are created)

// Bookings Components
// (Will be added when bookings-specific components are created)

// Saved Components
// (Will be added when saved-specific components are created)

// Trip Details Components
// (Will be added when trip details-specific components are created)

// Auth Components
// (Will be added when auth-specific components are created)

// Onboarding Components
// (Will be added when onboarding-specific components are created)